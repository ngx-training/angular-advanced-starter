import { InputFloatDirective } from './input-float.directive';

describe('InputFloatDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFloatDirective();
    expect(directive).toBeTruthy();
  });
});
